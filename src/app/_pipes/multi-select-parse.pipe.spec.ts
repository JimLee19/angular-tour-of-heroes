import { MultiSelectParsePipe } from './multi-select-parse.pipe';

describe('MultiSelectParsePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiSelectParsePipe();
    expect(pipe).toBeTruthy();
  });
});
